import { Metadata } from "next";
import { redirect } from "next/navigation";

export const metadata: Metadata = {
  title: "Our Ministry - Joel",
};
export default function Page() {
  return <div>FEATURED</div>;
}
