import React from "react";
import Banner, {
  IBannerProps,
} from "../../../app/components/shared/Banner/Banner";

export default function FullWidthImage(props: IBannerProps) {
  return <Banner {...props} />;
}
